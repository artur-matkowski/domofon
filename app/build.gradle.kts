// Top-level build file.
// Note: AGP 9 ships built-in Kotlin support. Applying org.jetbrains.kotlin.android
// alongside it is a hard error, so it is deliberately absent here.
plugins {
    id("com.android.application") version "9.0.0" apply false
}
