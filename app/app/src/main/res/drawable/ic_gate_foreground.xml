<?xml version="1.0" encoding="utf-8"?>
<!-- Foreground layer of the adaptive launcher icon: the ic_gate art, unchanged, placed on
     the 108x108 adaptive canvas. Scaled x2 (24 -> 48 units) and translated to 30,30 so it
     sits centred at 54,54 and well inside the 72-unit safe zone — a launcher may mask,
     shift or parallax anything outside it. Doubles as the <monochrome> layer: the system
     tints it, so a flat white silhouette is exactly what that slot wants. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <group
        android:scaleX="2"
        android:scaleY="2"
        android:translateX="30"
        android:translateY="30">
        <path
            android:fillColor="#FFFFFFFF"
            android:pathData="M3,6h18v2h-18zM3,16h18v2h-18zM5,8h2v8h-2zM9,8h2v8h-2zM13,8h2v8h-2zM17,8h2v8h-2z"/>
    </group>
</vector>
