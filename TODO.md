User specified todo list. remove entry after finish up with it

1) icon on head unit is just white. the white 3 bars visible on the phone, are not visible on white background. (this happens everywhere where whie background is aplied)
2) on phone the screenshot refreshing mechanizm is implemented in sutch a way that it looks like for one frame the image is gone. something like a glitch.
3) phone has 3 buttons, fullscreen headunit variant should have that too. only popup, on aproaching fence gate, should have single button
4) on car head unit. whenever a refresh is suppouse to happen (every X sec) the whole head unit blinks for a moment. it does not look like glitch, more like app being disabled, and re enabeld right after. the color tones down to darkness, in something that looks like 200ms smooth animation, then goes back up, right with new content (image, and texts)